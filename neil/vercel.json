{
  "build": {
    "env": {
      "PRISMA_GENERATE_CMD": "npx prisma generate"
    },
    "scripts": {
      "build": "npm run build",
      "prisma": "${PRISMA_GENERATE_CMD} && npm run build"
    }
  }
  
}